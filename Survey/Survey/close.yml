info:
  name: close
  type: http
  seq: 10

http:
  method: POST
  url: "{{url}}/api/v1/survey/close"
  body:
    type: json
    data: |-
      {
          "id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
