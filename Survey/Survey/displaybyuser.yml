info:
  name: displaybyuser
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/v1/survey/displaybyuser"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
