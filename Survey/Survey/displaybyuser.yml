info:
  name: displaybyuser
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/v1/survey/displaybyuser"
  body:
    type: json
    data: |-
      {
          "userStatus": "New",//"Completed", //null,
          "paging": {
              "page": 1,
              "perPage":10
          }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
