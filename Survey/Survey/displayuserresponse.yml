info:
  name: displayuserresponse
  type: http
  seq: 17

http:
  method: POST
  url: "{{url}}/api/v1/survey/displayuserresponse"
  body:
    type: json
    data: |-
      {
        "Id": 25,
        "UserId": 10005
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
