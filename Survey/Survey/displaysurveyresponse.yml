info:
  name: displaysurveyresponse
  type: http
  seq: 14

http:
  method: POST
  url: "{{url}}/api/v1/survey/displaysurveyresponse"
  body:
    type: json
    data: |-
      {
        "Id": 21
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
