info:
  name: publish
  type: http
  seq: 11

http:
  method: POST
  url: "{{url}}/api/v1/survey/publish"
  body:
    type: json
    data: |-
      {
          "id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
