info:
  name: publish
  type: http
  seq: 12

http:
  method: POST
  url: "{{url}}/api/v1/survey/publish"
  body:
    type: json
    data: |-
      {
          "id": 25
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
