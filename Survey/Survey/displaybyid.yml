info:
  name: displaybyid
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/api/v1/survey/displaybyid"
  body:
    type: json
    data: |-
      {
        "Id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
