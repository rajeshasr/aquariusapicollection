info:
  name: activate
  type: http
  seq: 8

http:
  method: POST
  url: "{{url}}/api/v1/survey/activate"
  body:
    type: json
    data: |-
      {
          "id": 20
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
