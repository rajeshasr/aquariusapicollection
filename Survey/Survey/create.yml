info:
  name: create
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/api/v1/survey/create"
  body:
    type: json
    data: |-
      {
        "SiteId": 1001,
        "Title": "Software Survey",
        "Description": "Survey Software Testing",
        "EmailSubject": "Survey Software Subject",
        "EstimatedTimeMins": 5,
        "ExpiresAt": null, //"2026-05-26T00:00:00Z",
        "SurveyRoleIds": [5, 10, 11]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
