info:
  name: create
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/api/v1/survey/create"
  body:
    type: json
    data: |-
      {
        "SiteId": 1001,
        "Title": "Aquarius Survey",
        "Description": "Survey Software Testing",
        "EmailSubject": "Survey Software Subject",
        "EstimatedTimeMins": 10,
        "ExpiresAt": "2026-05-26T00:00:00Z",
        "SurveyRoleIds": [5, 10]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
