info:
  name: deactivate
  type: http
  seq: 9

http:
  method: POST
  url: "{{url}}/api/v1/survey/deactivate"
  body:
    type: json
    data: |-
      {
          "id": 20
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
