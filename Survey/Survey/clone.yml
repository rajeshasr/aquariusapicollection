info:
  name: clone
  type: http
  seq: 7

http:
  method: POST
  url: "{{url}}/api/v1/survey/clone"
  body:
    type: json
    data: |-
      {
        "id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
