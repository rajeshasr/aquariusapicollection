info:
  name: display
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/v1/survey/surveystatus/display"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
