info:
  name: displaybyunit
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/v1/fdinstruction/displaybyunit"
  body:
    type: json
    data: "{\r

      \    \"unitId\": 1\r

      }\r

      //SHOW ONLY CURRENT, NO EXPIRED DATA"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
