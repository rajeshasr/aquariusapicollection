info:
  name: deactivate
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/api/v1/vehicle/deactivate"
  body:
    type: json
    data: "{\r\n    \"id\": 7\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
