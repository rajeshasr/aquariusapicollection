info:
  name: send
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/v1/notification/send"
  body:
    type: json
    data: |-
      {
        "id": 1,
        "title": "Test for All",
        "message": "Water supply will stop at 5 PM"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
