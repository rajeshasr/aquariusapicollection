info:
  name: sendtouser
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/v1/notification/sendtouser"
  body:
    type: json
    data: |-
      {
        "id": 1,
        "userId": 10005,
        "title": "Maintenance",
        "message": "Water supply will stop at 5 PM"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
