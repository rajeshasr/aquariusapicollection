info:
  name: reject
  type: http
  seq: 11

http:
  method: POST
  url: "{{url}}/api/v1/advertisement/reject"
  body:
    type: json
    data: "{\r\n    \"id\": 2\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
