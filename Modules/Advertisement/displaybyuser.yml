info:
  name: displaybyuser
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/api/v1/advertisement/displaybyuser"
  body:
    type: json
    data: "{\r\n    \"userId\": 10002\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
