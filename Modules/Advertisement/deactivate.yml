info:
  name: deactivate
  type: http
  seq: 13

http:
  method: POST
  url: "{{url}}/api/v1/advertisement/deactivate"
  body:
    type: json
    data: "{\r\n    \"id\": 100011\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
