info:
  name: displaybysite-expired
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/api/v1/advertisement/displaybysite-expired"
  body:
    type: json
    data: "{\r\n    \"siteId\": 1001\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
