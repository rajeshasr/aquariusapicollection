info:
  name: renew
  type: http
  seq: 12

http:
  method: POST
  url: "{{url}}/api/v1/advertisement/renew"
  body:
    type: json
    data: "{\r\n    \"id\": 10004\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
