info:
  name: displaybyunit
  type: http
  seq: 8

http:
  method: POST
  url: "{{url}}/api/v1/package/displaybyunit"
  body:
    type: json
    data: "{\r

      \    \"unitId\": 11,\r

      \    \"incoming\": null,\r

      \    \"released\": null\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
