info:
  name: updatesignature
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/api/v1/package/updatesignature"
  body:
    type: json
    data: "{\r

      \    \"id\": 1,\r

      \    \"signature\": \"Signature\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
