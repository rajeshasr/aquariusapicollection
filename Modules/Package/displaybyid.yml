info:
  name: displaybyid
  type: http
  seq: 10

http:
  method: POST
  url: "{{url}}/api/v1/package/displaybyid"
  body:
    type: json
    data: "{\r\n    \"id\": 5\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
