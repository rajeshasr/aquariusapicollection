info:
  name: setdefault
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/api/v1/projecttheme/setdefault"
  body:
    type: json
    data: "{\r\n    \"id\": 4\r\n}\r\n"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
