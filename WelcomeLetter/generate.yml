info:
  name: generate
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/api/v1/notification/welcomeletter/generate"
  body:
    type: json
    data: |-
      {
        "roleIds": [5],
        "unitIds": [],
        "userIds": [],
        "isEmail": true,
        "isPrint": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
