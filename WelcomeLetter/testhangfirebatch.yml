info:
  name: testhangfirebatch
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/api/v1/notification/welcomeletter/testhangfirebatch"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
