info:
  name: 1-displayactivewithfilesbysite
  type: http
  seq: 11

http:
  method: POST
  url: "{{url}}/api/v1/library/libraryfolder/displayactivewithfilesbysite"
  body:
    type: json
    data: "{\r\n    \"siteId\": 1001\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
