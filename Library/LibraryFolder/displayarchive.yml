info:
  name: displayarchive
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/api/v1/library/libraryfolder/displayarchive"
  body:
    type: json
    data: "{\r\n    \"siteId\": 1001\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
