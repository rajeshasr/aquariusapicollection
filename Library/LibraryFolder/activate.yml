info:
  name: activate
  type: http
  seq: 8

http:
  method: POST
  url: "{{url}}/api/v1/library/libraryfolder/activate"
  body:
    type: json
    data: "{\r\n    \"id\": 5\r\n}\r\n"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
