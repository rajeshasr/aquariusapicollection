info:
  name: restore
  type: http
  seq: 10

http:
  method: POST
  url: "{{url}}/api/v1/library/libraryfolder/restore"
  body:
    type: json
    data: "{\r

      \    \"Id\": 7,\r

      \    \"ArchivedBy\": 10001\r

      }\r\n"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
