info:
  name: create
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/api/v1/library/libraryfolder/create"
  body:
    type: json
    data: "{\r

      \    \"siteId\": 1001,\r

      \    \"folderName\": \"Folder 13\",\r

      \    \"folderDesc\": \"Folder Desc 13\",\r

      \    \"parentId\": 2\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
