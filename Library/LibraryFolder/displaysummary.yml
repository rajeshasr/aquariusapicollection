info:
  name: displaysummary
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/v1/library/libraryfolder/displaysummary"
  body:
    type: json
    data: "{\r\n    \"siteId\": 1001\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
