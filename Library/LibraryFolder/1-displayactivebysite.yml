info:
  name: 1-displayactivebysite
  type: http
  seq: 13

http:
  method: POST
  url: "{{url}}/api/v1/library/libraryfolder/displayactivebysite"
  body:
    type: json
    data: "{\r\n    \"siteId\": 1001\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
