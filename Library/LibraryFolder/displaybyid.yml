info:
  name: displaybyid
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/api/v1/library/libraryfolder/displaybyid"
  body:
    type: json
    data: "{\r\n    \"id\": 8\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
