info:
  name: archive
  type: http
  seq: 9

http:
  method: POST
  url: "{{url}}/api/v1/library/libraryfolder/archive"
  body:
    type: json
    data: "{\r

      \    \"Id\": 8,\r

      \    \"ArchivedBy\": 10001\r

      }\r\n"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
