info:
  name: updatemove
  type: http
  seq: 7

http:
  method: POST
  url: "{{url}}/api/v1/library/libraryfolder/updatemove"
  body:
    type: json
    data: "{\r

      \    \"id\": 4,\r

      \    \"parentId\": 0\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
