info:
  name: updatemove
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/api/v1/library/libraryfiles/updatemove"
  body:
    type: json
    data: "{\r

      \    \"Id\": 17,\r

      \    \"FolderId\": 8\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
