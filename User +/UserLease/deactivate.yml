info:
  name: deactivate
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/api/v1/userlease/deactivate"
  body:
    type: json
    data: "{\r\n    \"id\": 3\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
