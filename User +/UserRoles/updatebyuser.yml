info:
  name: updatebyuser
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/v1/userroles/updatebyuser"
  body:
    type: json
    data: "    {\r

      \        \"userId\": 10012,\r

      \        \"roleId\": [10, 11, 188]\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
