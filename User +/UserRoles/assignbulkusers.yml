info:
  name: assignbulkusers
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/api/v1/userroles/assignbulkusers"
  body:
    type: json
    data: "{\r

      \    \"roleId\": 10,\r

      \    \"userId\": [10012, 10013]\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
