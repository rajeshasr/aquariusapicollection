info:
  name: displaysiteadmin
  type: http
  seq: 8

http:
  method: POST
  url: "{{url}}/api/v1/user/displaysiteadmin"
  body:
    type: json
    data: "{\r\n    \"siteId\": 1003\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
