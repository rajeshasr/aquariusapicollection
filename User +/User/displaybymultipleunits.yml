info:
  name: displaybymultipleunits
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/api/v1/user/displaybymultipleunits"
  body:
    type: json
    data: |-
      {
          "UnitIds": [3]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
