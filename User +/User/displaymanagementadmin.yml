info:
  name: displaymanagementadmin
  type: http
  seq: 7

http:
  method: POST
  url: "{{url}}/api/v1/user/displaymanagementadmin"
  body:
    type: json
    data: |-
      {
          "managementId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
