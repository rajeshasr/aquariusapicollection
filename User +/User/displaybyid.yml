info:
  name: displaybyid
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/api/v1/user/displaybyid"
  body:
    type: json
    data: "{\r\n    \"id\": 10005\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
