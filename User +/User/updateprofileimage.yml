info:
  name: updateprofileimage
  type: http
  seq: 23

http:
  method: POST
  url: "{{url}}/api/v1/user/updateprofileimage"
  body:
    type: multipart-form
    data:
      - name: id
        type: text
        value: "10017"
      - name: profileImageFile
        type: file
        value:
          - /C:/Users/Genesis/Pictures/1.jpg

runtime:
  scripts:
    - type: after-response
      code: "// Stores the accessToken in an environment or global variable\r

        var accessToken = res.getBody().data.accessToken;\r

        bru.setVar(\"accessToken\", accessToken);"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
