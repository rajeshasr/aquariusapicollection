info:
  name: displaybysiteroletype
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/v1/user/displaybysiteroletype"
  body:
    type: json
    data: "{\r

      \    \"siteId\": 1001,\r

      \    \"search\": \"STAFF\" \r

      \    // --ALL--STAFF--RESIDENTS\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
