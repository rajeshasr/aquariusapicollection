info:
  name: resetpassword
  type: http
  seq: 20

http:
  method: POST
  url: "{{url}}/api/v1/user/resetpassword"
  body:
    type: json
    data: "{\r

      \    \"username\": \"10001\",\r

      \    \"newpassword\": \"test\"\r

      }"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
