info:
  name: userselectsite
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/api/v1/usersites/userselectsite"
  body:
    type: json
    data: |-
      {
          "userId": 10005,
          "siteId": 1001
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
