info:
  name: displaybyuserid
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/v1/userphone/displaybyuserid"
  body:
    type: json
    data: "{\r\n    \"userid\": 10001\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
