info:
  name: deactivate
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/api/v1/userphone/deactivate"
  body:
    type: json
    data: "{\r\n    \"id\": 37\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
