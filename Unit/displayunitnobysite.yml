info:
  name: displayunitnobysite
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/v1/unit/displayunitnobysite"
  body:
    type: json
    data: "{\r\n    \"siteId\": 1003\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
