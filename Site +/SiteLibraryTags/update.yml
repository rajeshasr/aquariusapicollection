info:
  name: update
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/api/v1/sitelibrarytags/update"
  body:
    type: json
    data: "{\r

      \    \"id\": 5,\r

      \    \"siteId\": 1001,\r

      \    \"tagName\": \"Test 12\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
