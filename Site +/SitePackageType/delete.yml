info:
  name: delete
  type: http
  seq: 7

http:
  method: POST
  url: "{{url}}/api/v1/sitepackagetype/delete"
  body:
    type: json
    data: "{\r\n    \"id\": 1\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
