info:
  name: create
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/api/v1/sitelockertype/create"
  body:
    type: json
    data: "{\r

      \    \"siteId\": 1001,\r

      \    \"lockerType\": \"Test 1\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
