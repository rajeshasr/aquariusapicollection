info:
  name: displaybysite
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/v1/sitelockertype/displaybysite"
  body:
    type: json
    data: "{\r\n    \"siteID\": 1001\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
