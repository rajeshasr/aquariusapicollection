info:
  name: update
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/api/v1/sitelockertype/update"
  body:
    type: json
    data: "{\r

      \    \"id\": 3,\r

      \    \"siteId\": 1001,\r

      \    \"lockerType\": \"Test 11\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
