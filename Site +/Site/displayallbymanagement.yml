info:
  name: displayallbymanagement
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/v1/site/displayallbymanagement"
  body:
    type: json
    data: "{\r\n    \"managementId\": 2\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
