info:
  name: create
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/api/v1/siteauthcategory/create"
  body:
    type: json
    data: "{\r

      \    \"siteId\": 1001,\r

      \    \"catName\": \"Test 2\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
