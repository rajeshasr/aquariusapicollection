info:
  name: displayall
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/v1/siteterms/displayall"
  body:
    type: json
    data: "{\r\n    \"SiteId\": 1001\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
