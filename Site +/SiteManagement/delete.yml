info:
  name: delete
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/api/v1/sitemanagement/delete"
  body:
    type: json
    data: "{\r\n    \"id\": 3\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
