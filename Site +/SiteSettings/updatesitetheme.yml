info:
  name: updatesitetheme
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/api/v1/sitesettings/updatesitetheme"
  body:
    type: json
    data: "{\r

      \    \"id\": 3,\r

      \    \"themeId\": 2\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
