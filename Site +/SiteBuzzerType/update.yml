info:
  name: update
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/api/v1/sitebuzzertype/update"
  body:
    type: json
    data: "{\r

      \    \"id\": 6,\r

      \    \"siteId\": 1001,\r

      \    \"buzzerType\": \"Test 33\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
