info:
  name: create
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/api/v1/sitebuzzertype/create"
  body:
    type: json
    data: "{\r

      \    \"siteId\": 1001,\r

      \    \"buzzerType\": \"Test 3\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
