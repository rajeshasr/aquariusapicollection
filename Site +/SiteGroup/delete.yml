info:
  name: delete
  type: http
  seq: 8

http:
  method: POST
  url: "{{url}}/api/v1/sitegroup/delete"
  body:
    type: json
    data: "{\r\n    \"id\": 1\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
