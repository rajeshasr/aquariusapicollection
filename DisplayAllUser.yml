info:
  name: DisplayAllUser
  type: http
  seq: 23

http:
  method: POST
  url: "{{url}}/api/v1/user/displayall"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
