info:
  name: activate
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/api/v1/sr/vendor/activate"
  body:
    type: json
    data: "{\r\n    \"id\": 1005\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
