info:
  name: displaybysite
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/v1/rbac/role/displaybysite"
  body:
    type: json
    data: "{\r\n    \"siteId\": 1001\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
