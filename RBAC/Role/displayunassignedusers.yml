info:
  name: displayunassignedusers
  type: http
  seq: 8

http:
  method: POST
  url: "{{url}}/api/v1/rbac/role/displayunassignedusers"
  body:
    type: json
    data: "{\r\n    \"roleId\": 11\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
