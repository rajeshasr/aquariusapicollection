info:
  name: displaybyid
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/api/v1/rbac/role/displaybyid"
  body:
    type: json
    data: "{\r\n    \"id\": 5\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
