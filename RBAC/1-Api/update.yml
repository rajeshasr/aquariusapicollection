info:
  name: update
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/api/v1/rbac/api/update"
  body:
    type: json
    data: "{\r

      \    \"id\": 3,\r

      \    \"apiName\": \"LightBlue\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
