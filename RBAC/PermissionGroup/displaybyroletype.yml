info:
  name: displaybyroletype
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/v1/rbac/permissiongroup/displaybyroletype"
  body:
    type: json
    data: "{\r

      \    // \"RoleType\": \"Staff\"\r

      \     \"RoleType\": \"Resident\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
