info:
  name: DisplayAllSite
  type: http
  seq: 20

http:
  method: POST
  url: "{{url}}/api/v1/site/displayall"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
