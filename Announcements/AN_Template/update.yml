info:
  name: update
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/api/v1/an/template/update"
  body:
    type: json
    data: |-
      {
        "id": 2,
        "TemplateName": "Announcement Template 22",
        "Title": "Announcement 22",
        "Body": "Body 22",
        "CoverImageURL": "url 22"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
