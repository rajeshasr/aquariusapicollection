info:
  name: create
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/api/v1/an/template/create"
  body:
    type: json
    data: |-
      {
        "TemplateName": "Announcement Template 3",
        "Title": "Announcement 3",
        "Body": "Body 3",
        "CoverImageURL": "url 3"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
