info:
  name: update
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/api/v1/an/sitecategory/update"
  body:
    type: json
    data: |-
      {
          "id": 8,
          "CatName": "Test 1123",
          "ColorCode": "#00FF00"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
