info:
  name: create
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/api/v1/an/sitecategory/create"
  body:
    type: json
    data: |-
      {
          "CatName": "Test",
          "ColorCode": "#00FF00"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
