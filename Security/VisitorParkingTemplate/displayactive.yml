info:
  name: displayactive
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/v1/visitorparking/parkingtemplate/displayactive"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
