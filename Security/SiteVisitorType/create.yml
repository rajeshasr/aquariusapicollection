info:
  name: create
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/api/v1/sitevisitortype/create"
  body:
    type: json
    data: |-
      {
          "siteId": 1001,
          "visitorType": "Test",
          "colorCode": "#F9F9F9"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
