info:
  name: displaysitekeysbysite
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/v1/sitekeycheckout/displaysitekeysbysite"
  body:
    type: json
    data: "{\r\n    \"siteId\": 1001\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
