info:
  name: displaybyid
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/api/v1/visitorparking/parkingpass/displaybyid"
  body:
    type: json
    data: |-
      {
        "Id": 12
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
