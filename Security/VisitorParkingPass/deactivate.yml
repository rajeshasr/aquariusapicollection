info:
  name: deactivate
  type: http
  seq: 9

http:
  method: POST
  url: "{{url}}/api/v1/visitorparking/parkingpass/deactivate"
  body:
    type: json
    data: |-
      {
          "id": 14
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
