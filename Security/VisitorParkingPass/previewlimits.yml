info:
  name: previewlimits
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/v1/visitorparking/parkingpass/previewlimits"
  body:
    type: json
    data: |-
      {
        "UnitId": 1,
        "LicensePlateNo": "ABC123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
