info:
  name: displaybyunit
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/api/v1/visitorparking/parkingpass/displaybyunit"
  body:
    type: json
    data: |-
      {
        "UnitId": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
