info:
  name: displayactivepass
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/api/v1/visitorparking/parkingpass/displayactivepass"
  body:
    type: json
    data: |-
      {
        "UnitId": 1,
        "LicensePlateNo": "ABC123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
