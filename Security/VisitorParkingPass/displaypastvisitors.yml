info:
  name: displaypastvisitors
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/v1/visitorparking/parkingpass/displaypastvisitors"
  body:
    type: json
    data: |-
      {
        "UnitId": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
