info:
  name: update
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/api/v1/siteviolationtype/update"
  body:
    type: json
    data: |-
      {
          "id": 5,
          "siteId": 1001,
          "violationType": "Test 1",
          "colorCode": "#FF0000"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
