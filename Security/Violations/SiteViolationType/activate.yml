info:
  name: activate
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/api/v1/sitevisitortype/activate"
  body:
    type: json
    data: |-
      {
          "id": 4
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
