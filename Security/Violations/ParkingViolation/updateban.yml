info:
  name: updateban
  type: http
  seq: 7

http:
  method: POST
  url: "{{url}}/api/v1/visitorparking/parkingviolation/updateban"
  body:
    type: json
    data: |-
      {
        "Id": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
