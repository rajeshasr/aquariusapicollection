info:
  name: deletefile
  type: http
  seq: 10

http:
  method: POST
  url: "{{url}}/api/v1/visitorparking/parkingviolation/deletefile"
  body:
    type: json
    data: |-
      {
          "id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
