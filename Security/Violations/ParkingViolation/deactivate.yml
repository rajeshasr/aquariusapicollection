info:
  name: deactivate
  type: http
  seq: 9

http:
  method: POST
  url: "{{url}}/api/v1/visitorparking/parkingviolation/deactivate"
  body:
    type: json
    data: |-
      {
          "id": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
