info:
  name: displaybysite
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/v1/visitorparking/parkingviolation/displaybysite"
  body:
    type: json
    data: |-
      {
        "siteId": 1001,
        "search": null,
        "dateRangeId": 4,
        "customStartDate": null,
        "customEndDate": null,
        "paging": {
          "page": 1,
          "perPage": 10
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
