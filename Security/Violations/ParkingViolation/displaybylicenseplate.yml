info:
  name: displaybylicenseplate
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/v1/visitorparking/parkingviolation/displaybylicenseplate"
  body:
    type: json
    data: |-
      {
        "LicensePlateNo": "ABC123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
