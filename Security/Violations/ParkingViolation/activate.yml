info:
  name: activate
  type: http
  seq: 8

http:
  method: POST
  url: "{{url}}/api/v1/visitorparking/parkingviolation/activate"
  body:
    type: json
    data: |-
      {
          "id": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
