info:
  name: deletefile
  type: http
  seq: 8

http:
  method: POST
  url: "{{url}}/api/v1/visitorparking/otherviolation/deletefile"
  body:
    type: json
    data: |-
      {
          "id": 13
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
