info:
  name: displaybyid
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/api/v1/visitorparking/otherviolation/displaybyid"
  body:
    type: json
    data: |-
      {
        "Id": 1015
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
