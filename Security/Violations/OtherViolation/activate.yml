info:
  name: activate
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/api/v1/visitorparking/otherviolation/activate"
  body:
    type: json
    data: |-
      {
          "id": 1015
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
