info:
  name: deactivate
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/api/v1/visitorparking/otherviolationupdate/deactivate"
  body:
    type: json
    data: |-
      {
          "id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
