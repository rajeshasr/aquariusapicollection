info:
  name: create
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/api/v1/siteotherviolationtype/create"
  body:
    type: json
    data: |-
      {
        "siteId": 1001,
        "violationType": "Test 3",
        "colorCode": "#F9F9F9",
        "NoticeDescription": "NoticeDescription",
        "Rules": "Rules"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
