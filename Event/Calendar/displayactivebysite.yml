info:
  name: displayactivebysite
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/v1/event/calendar/displayactivebysite"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
