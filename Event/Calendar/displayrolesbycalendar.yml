info:
  name: displayrolesbycalendar
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/api/v1/event/calendar/displayrolesbycalendar"
  body:
    type: json
    data: |-
      {
          "Id": 4
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
