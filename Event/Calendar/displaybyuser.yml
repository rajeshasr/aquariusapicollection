info:
  name: displaybyuser
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/api/v1/event/calendar/displaybyuser"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
