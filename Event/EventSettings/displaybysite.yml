info:
  name: displaybysite
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/v1/event/eventsettings/displaybysite"
  body:
    type: json
    data: |-
      {
          "siteID": 1001
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
