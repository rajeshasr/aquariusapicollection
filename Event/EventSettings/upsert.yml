info:
  name: upsert
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/v1/event/eventsettings/upsert"
  body:
    type: json
    data: |-
      {
          "siteId": 1001,
          "sendEmailDefault": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
