info:
  name: deactivate
  type: http
  seq: 7

http:
  method: POST
  url: "{{url}}/api/v1/event/events/deactivate"
  body:
    type: json
    data: |-
      {
          "id": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
