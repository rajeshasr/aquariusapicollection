info:
  name: displaybyid
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/api/v1/event/events/displaybyid"
  body:
    type: json
    data: |-
      {
          "Id": 4
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
