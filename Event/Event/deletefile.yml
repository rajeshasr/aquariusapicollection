info:
  name: deletefile
  type: http
  seq: 9

http:
  method: POST
  url: "{{url}}/api/v1/event/events/deletefile"
  body:
    type: json
    data: "{\r\n    \"id\": 3\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
