info:
  name: DisplayAllUnit
  type: http
  seq: 21

http:
  method: POST
  url: "{{url}}/api/v1/unit/displayall"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
