info:
  name: images
  type: http
  seq: 22

http:
  method: GET
  url: "{{url}}/api/v1/images/servicerequests/243.jpg"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
