info:
  name: DisplayAllManagement
  type: http
  seq: 19

http:
  method: POST
  url: "{{url}}/api/v1/management/displayall"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
