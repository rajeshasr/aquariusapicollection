info:
  name: update
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/api/v1/masters/advtcategory/update"
  body:
    type: json
    data: "{\r

      \    \"id\": 4,\r

      \    \"catName\": \"Parking wanted 1\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
