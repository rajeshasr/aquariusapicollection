info:
  name: displayactive
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/v1/masters/librarytags/displayactive"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
