info:
  name: activate
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/api/v1/masters/idtype/activate"
  body:
    type: json
    data: "{\r\n    \"id\": 7\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
