info:
  name: displayall
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/v1/masters/unitownertype/displayall"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
