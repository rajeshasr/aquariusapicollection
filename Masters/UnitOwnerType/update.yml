info:
  name: update
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/api/v1/masters/unitownertype/update"
  body:
    type: json
    data: "{\r

      \    \"id\": 5,\r

      \    \"ownerType\": \"Test 22\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
