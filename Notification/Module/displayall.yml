info:
  name: displayall
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/v1/notification/notimodule/displayall"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
